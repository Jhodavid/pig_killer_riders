
import 'package:flutter/material.dart';

class AccountStatementsAdmin extends StatelessWidget {
  static const String route = '/account-statements-admin';

  const AccountStatementsAdmin({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
